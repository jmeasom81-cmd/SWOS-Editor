import fs from 'node:fs';
import crypto from 'node:crypto';

const read=p=>{if(!fs.existsSync(p))throw new Error(`League One identity validation: missing ${p}.`);return JSON.parse(fs.readFileSync(p,'utf8'));},sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const manifest=read('football-db/manifest.json'),identities=read('football-db/identities.json'),packs=read('football-db/research-packs.json'),coverage=read('football-db/coverage.json'),model=read('football-db/squad-model.json'),schema=read('football-db/schema-v1.json'),queue=read('football-db/league-one-identity-expansion-queue.json'),intake=read('football-db/league-one-identity-intake.json');
const checks=[],check=(id,condition,detail)=>{checks.push({id,pass:!!condition,detail});if(!condition)throw new Error(`League One identity validation failed [${id}]: ${detail}`);};
const divisionById=new Map((coverage.clubs||[]).map(c=>[c.id,c.division])),entries=Object.entries(identities.clubs||{}),l1=entries.filter(([id])=>divisionById.get(id)===2),l1Count=l1.length,l1Players=l1.reduce((n,[,rows])=>n+(rows?.length||0),0),expectedClubs=44+l1Count,expectedPlayers=759+l1Count*16;
check('season',manifest.season==='2026/27'&&identities.season==='2026/27'&&packs.season==='2026/27','season must remain 2026/27');
check('division-structure',(coverage.clubs||[]).filter(c=>c.division===2).length===24,'League One must contain 24 mapped clubs');
check('upper-identities',identities.divisionCoverage?.premierLeague?.clubs===20&&identities.divisionCoverage?.premierLeague?.players===375&&identities.divisionCoverage?.championship?.clubs===24&&identities.divisionCoverage?.championship?.players===384,'Premier League and Championship identity foundations must remain intact');
check('league-one-range',l1Count>=1&&l1Count<=24,`League One identity coverage ${l1Count}/24`);
check('league-one-feed',identities.divisionCoverage?.leagueOne?.clubs===l1Count&&identities.divisionCoverage?.leagueOne?.players===l1Players,`feed ${identities.divisionCoverage?.leagueOne?.clubs}/${identities.divisionCoverage?.leagueOne?.players}, actual ${l1Count}/${l1Players}`);
check('league-two-blocked',(identities.divisionCoverage?.leagueTwo?.clubs||0)===0,'League Two identity publication remains blocked');
check('england-counts',identities.clubCount===expectedClubs&&identities.playerCount===expectedPlayers,`England identities ${identities.clubCount}/${identities.playerCount}; expected ${expectedClubs}/${expectedPlayers}`);
check('manifest-counts',manifest.coverage?.englandClubs?.identityReadyClubs===expectedClubs&&manifest.coverage?.englandClubs?.identityPlayers===expectedPlayers&&manifest.coverage?.leagueOne?.identityReadyClubs===l1Count&&manifest.coverage?.leagueOne?.identityPlayers===l1Players,'manifest identity totals must match the feed');
check('database-version',manifest.version===`2026.27-england-identity.${expectedClubs}`,`manifest version ${manifest.version}`);
const researchByDivision={0:0,1:0,2:0,3:0};let researchPlayers=0;for(const [id,club] of Object.entries(packs.clubs||{})){researchByDivision[divisionById.get(id)]++;researchPlayers+=Object.keys(club.players||{}).length;}
check('research-protected',researchByDivision[0]===20&&researchByDivision[1]===24&&researchByDivision[2]===0&&researchByDivision[3]===0&&packs.packCount===44&&packs.playerCount===704&&researchPlayers===704,`research divisions ${JSON.stringify(researchByDivision)}; packs ${packs.packCount}/${packs.playerCount}`);
check('queue',queue.totals?.identityReady===l1Count&&queue.totals?.evidenceRequired===24-l1Count,`queue ready ${queue.totals?.identityReady}, pending ${queue.totals?.evidenceRequired}`);
check('intake',intake.totals?.identityReady===l1Count&&intake.totals?.promotionReadyClubs===24-l1Count,`intake ready ${intake.totals?.identityReady}, promotion-ready ${intake.totals?.promotionReadyClubs}`);
check('write-lock',manifest.installation?.teamWriteReady===false&&manifest.installation?.careerWriteReady===false&&model.writeSafety?.teamWriteEnabled===false&&model.writeSafety?.careerWriteEnabled===false,'binary writes remain locked');
check('schema-16',schema.properties?.squad?.properties?.swos16PlayerIds?.minItems===16&&schema.properties?.squad?.properties?.swos16PlayerIds?.maxItems===16,'SWOS squad capacity remains exactly 16');
for(const [clubId,rows] of l1){const names=new Set((rows||[]).map(p=>String(p?.footballName||'').trim().toLocaleLowerCase('en'))),roles={Goalkeeper:0,Defender:0,Midfielder:0,Forward:0};check(`${clubId}-size`,Array.isArray(rows)&&rows.length===16,`${rows?.length||0}/16 rows`);check(`${clubId}-unique`,names.size===16,`${names.size}/16 unique names`);for(const p of rows||[]){check(`${clubId}-${p.footballName}-source`,!!String(p.source||'').trim(),'source required');check(`${clubId}-${p.footballName}-nationality`,!!String(p.nationality||'').trim(),'nationality required');check(`${clubId}-${p.footballName}-role`,Object.hasOwn(roles,p.group),`role ${p.group}`);if(Object.hasOwn(roles,p.group))roles[p.group]++;}check(`${clubId}-mix`,roles.Goalkeeper>=2&&roles.Defender>=4&&roles.Midfielder>=4&&roles.Forward>=2,JSON.stringify(roles));}
const integrity={algorithm:'SHA-256',resources:{verifiedIdentities:{path:'football-db/identities.json',sha256:sha('football-db/identities.json')},researchPacks:{path:'football-db/research-packs.json',sha256:sha('football-db/research-packs.json')},squadModel:{path:'football-db/squad-model.json',sha256:sha('football-db/squad-model.json')},clubRecordSchema:{path:'football-db/schema-v1.json',sha256:sha('football-db/schema-v1.json')}}};
const result={schemaVersion:1,status:'pass',databaseId:manifest.databaseId,databaseVersion:manifest.version,season:manifest.season,validatedAt:new Date().toISOString(),scope:'England League One identity expansion with completed Premier League and Championship research protected',totals:{clubs:20,players:320,identityClubs:identities.clubCount,identityPlayers:identities.playerCount,championshipIdentityClubs:24,championshipIdentityPlayers:384,leagueOneIdentityClubs:l1Count,leagueOneIdentityPlayers:l1Players,u21Candidates:identities.u21PlayerCount,checks:checks.length},guarantees:{premierLeagueIdentityComplete:true,premierLeagueResearchComplete:true,championshipIdentityComplete:true,championshipResearchComplete:true,leagueOneIdentityExpansion:true,leagueOneIdentityReady:l1Count,resourceIntegrity:true,teamWriteEnabled:false,careerWriteEnabled:false},integrity,checks};
fs.writeFileSync('football-db/validation.json',JSON.stringify(result,null,2)+'\n','utf8');
console.log(`League One identity expansion validation PASS · England ${identities.clubCount}/92 identity clubs · League One ${l1Count}/24 · research protected at 44 clubs / 704 players.`);
